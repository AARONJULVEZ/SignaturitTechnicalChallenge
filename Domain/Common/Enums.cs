namespace Domain.Common
{
    public enum ComparisonStatus
    {
        Win,
        Draw,
        Lose
    }

    public enum RolesSignatureValue
    {
        King = 'K',
        Notary = 'N',
        Validator = 'V'
    }
}
